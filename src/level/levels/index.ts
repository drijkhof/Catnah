import type { LevelDefinition } from '../Level';
import { FOREST } from './forest';
import { CAVE } from './cave';
import { CITY } from './city';

/** The levels, in the order they are played. */
export const LEVELS: LevelDefinition[] = [FOREST, CAVE, CITY];
